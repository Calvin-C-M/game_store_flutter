import 'package:flutter/material.dart';

class MyGamesPage extends StatelessWidget {
  const MyGamesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("My Games Page")
    );
  }
}